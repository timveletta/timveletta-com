---
export interface Props {
	percentage: number;
}

const { percentage } = Astro.props;
---

<div class="w-full h-6 my-2 bg-gray-200 rounded-full">
	<div class="h-6 bg-primary rounded-full" style={`width: ${percentage}%`}>
	</div>
</div>
